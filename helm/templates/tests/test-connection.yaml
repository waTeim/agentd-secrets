apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "x-pass.fullname" . }}-test-connection"
  labels:
    {{- include "x-pass.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['--spider', '{{ include "x-pass.fullname" . }}:{{ .Values.service.port }}/healthz']
  restartPolicy: Never
