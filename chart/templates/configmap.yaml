apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "x-pass.fullname" . }}-config
  labels:
    {{- include "x-pass.labels" . | nindent 4 }}
data:
  config.yaml: |
    services:
      {{- .Values.serviceRegistry | toYaml | nindent 6 }}
