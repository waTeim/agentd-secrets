apiVersion: v1
kind: Service
metadata:
  name: {{ include "agentd-secrets.fullname" . }}
  labels:
    {{- include "agentd-secrets.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agentd-secrets.selectorLabels" . | nindent 4 }}
