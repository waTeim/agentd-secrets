apiVersion: v1
kind: Service
metadata:
  name: {{ include "x-pass.fullname" . }}
  labels:
    {{- include "x-pass.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "x-pass.selectorLabels" . | nindent 4 }}
