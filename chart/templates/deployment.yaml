apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "agentd-secrets.fullname" . }}
  labels:
    {{- include "agentd-secrets.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "agentd-secrets.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
      {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "agentd-secrets.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "agentd-secrets.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          env:
            - name: BROKER_LISTEN_ADDR
              value: ":8080"
            - name: BROKER_CONFIG_PATH
              value: "/etc/agentd-secrets/config.yaml"
            # -- Keycloak
            - name: KEYCLOAK_ISSUER_URL
              value: {{ .Values.keycloak.issuerURL | quote }}
            - name: KEYCLOAK_AUDIENCE
              value: {{ .Values.keycloak.audience | quote }}
            - name: KEYCLOAK_CLIENT_ID
              value: {{ .Values.keycloak.clientID | quote }}
            - name: KEYCLOAK_REALM
              value: {{ .Values.keycloak.realm | quote }}
            - name: KEYCLOAK_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.existingSecret | default (printf "%s-secrets" (include "agentd-secrets.fullname" .)) }}
                  key: KEYCLOAK_CLIENT_SECRET
            # -- Vault (OIDC auth — Vault CLI-style localhost flow)
            - name: VAULT_ADDR
              value: {{ .Values.vault.addr | quote }}
            - name: VAULT_OIDC_MOUNT
              value: {{ .Values.vault.oidcMount | quote }}
            - name: VAULT_OIDC_ROLE
              value: {{ .Values.vault.oidcRole | quote }}
            - name: VAULT_KV_MOUNT
              value: {{ .Values.vault.kvMount | quote }}
            - name: VAULT_WRAP_TTL
              value: {{ .Values.vault.wrapTTL | quote }}
            # -- OIDC localhost callback listener
            - name: OIDC_LOCAL_LISTEN_HOST
              value: {{ .Values.oidcCallback.listenHost | quote }}
            - name: OIDC_LOCAL_LISTEN_PORT
              value: {{ .Values.oidcCallback.listenPort | quote }}
            - name: OIDC_LOCAL_REDIRECT_URI
              value: {{ .Values.oidcCallback.redirectURI | quote }}
            # -- Encryption
            - name: WRAPTOKEN_ENC_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.existingSecret | default (printf "%s-secrets" (include "agentd-secrets.fullname" .)) }}
                  key: WRAPTOKEN_ENC_KEY
            # -- Headless OIDC login credentials
            - name: KEYCLOAK_USERNAME
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.existingSecret | default (printf "%s-secrets" (include "agentd-secrets.fullname" .)) }}
                  key: KEYCLOAK_USERNAME
            - name: KEYCLOAK_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.existingSecret | default (printf "%s-secrets" (include "agentd-secrets.fullname" .)) }}
                  key: KEYCLOAK_PASSWORD
            - name: KC_LOGIN_TIMEOUT
              value: {{ .Values.login.loginTimeout | quote }}
            - name: KC_DUO_TIMEOUT
              value: {{ .Values.login.duoTimeout | quote }}
            - name: PLAYWRIGHT_HEADLESS
              value: {{ .Values.playwright.headless | quote }}
            - name: PLAYWRIGHT_BROWSER
              value: {{ .Values.playwright.browser | quote }}
          volumeMounts:
            - name: config
              mountPath: /etc/agentd-secrets
              readOnly: true
            - name: tmp
              mountPath: /tmp
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      volumes:
        - name: config
          configMap:
            name: {{ include "agentd-secrets.fullname" . }}-config
        - name: tmp
          emptyDir:
            sizeLimit: 500Mi
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
